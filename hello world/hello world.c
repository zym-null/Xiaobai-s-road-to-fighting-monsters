#include <stdio.h>
int main(void)
{
	//第一个C程序，include是预处理指令，引入头文件
	/*一个C程序一定有且仅有一个main（）函数，程序从main函数开始运行
	* printf声明函数用于格式化输出在屏幕上
	* return 0表示回到整形0
	* 每一个语句后都要用;
	* stdio.h,表示头文件
	*/
	printf("hello wrold!\n");
	return 0;
}
